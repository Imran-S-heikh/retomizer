{
    "scripts": {
        "start": "cargo run -- --config ./example/retomizer.config.json --watch",
        "build": "cargo run -- --config ./example/retomizer.config.json",
        "test-argmap": "cargo test -p argmap"
    }
}